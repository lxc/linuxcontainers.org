language: python
python:
 - 3.3
 - 3.4
before_install:
 - sudo add-apt-repository ppa:ubuntu-lxc/daily -y
 - sudo apt-get update -qq
 - sudo apt-get install -qq man2html-base
 - pip install beautifulsoup4 jinja2 markdown pygments pep8
script: pep8 generate && python generate
notifications:
  irc:
    channels:
      - "chat.freenode.net#lxcontainers"
    use_notice: true
